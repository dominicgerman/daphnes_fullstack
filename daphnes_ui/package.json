{
  "name": "daphnes_ui",
  "version": "1.0.0",
  "description": "The files for the frontend of Daphnes bar app (v2)",
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build ./*.html --dist-dir ./dist",
    "postbuild": "rm -rf ./dist/imgs && cp -R ./imgs ./dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Dominic German",
  "license": "ISC",
  "dependencies": {
    "parcel": "^2.7.0"
  },
  "devDependencies": {
    "parcel-plugin-static-files-copy": "^2.6.0"
  }
}
